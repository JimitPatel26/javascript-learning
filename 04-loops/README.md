# JavaScript Loops 🚀

This project demonstrates different types of loops in JavaScript.

## 📚 Concepts Covered

- `for` loop
- `while` loop
- `do...while` loop
- `for...in` loop (objects)
- `for...of` loop (arrays & strings)

## 💻 Key Learnings

- Use `for` loop when iteration count is known
- Use `while` when condition-based looping
- `do...while` runs at least once
- `for...in` → object keys
- `for...of` → values

## ▶️ How to Run

1. Open `index.html`
2. Open browser console

## ✍️ Author

Jimit Patel